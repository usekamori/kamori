/**
 * This file is no longer used.
 *
 * express-api now integrates Kamori via the console shim:
 *   import { installShim } from "@usekamori/sdk";
 *   installShim({ url, token });
 *
 * See src/index.ts for the full integration pattern.
 */
