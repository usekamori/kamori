/**
 * This file is no longer used.
 *
 * next-app now integrates Kamori via two patterns:
 *   1. HTTP middleware (withKamori)  — src/middleware.ts
 *   2. Business events (KamoriClient) — src/app/api/checkout/route.ts
 *
 * See those files for the full integration patterns.
 */
